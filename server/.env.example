# Human Execution Protocol — Server Configuration

# Port to listen on
PORT=3402

# Secret used for evidence hash generation (CHANGE THIS IN PRODUCTION)
SERVER_SECRET=hxp-dev-secret-change-me

# Comma-separated list of valid agent API keys
AGENT_API_KEYS=dev-agent-key

# Comma-separated list of valid human tokens
HUMAN_TOKENS=dev-human-token
